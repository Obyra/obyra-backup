Objetivo: asegurar que al llamar gotoPaso(2|3|4) el pane correspondiente se vea siempre, sin depender de Bootstrap Tabs.

1) Marcar panes con data-wz-step

En el HTML, asegurá que cada pane tenga la clase .tab-pane y un atributo estable:

<!-- ejemplo -->
<div id="wizard-paso1" class="tab-pane show active" data-wz-step="1">…</div>
<div id="wizard-paso2" class="tab-pane"             data-wz-step="2">…</div>
<div id="wizard-paso3" class="tab-pane"             data-wz-step="3">…</div>
<div id="wizard-paso4" class="tab-pane"             data-wz-step="4">…</div>


(Si ya existen ids distintos, con data-wz-step alcanza; no es obligatorio renombrar los ids.)

2) Reemplazar gotoPaso por un fallback que SIEMPRE muestre el panel

En static/js/wizard.js:

(function ensureGotoPaso(){
  window.gotoPaso = function(step){
    // localizar el pane por orden de prioridad
    const pane = document.querySelector(
      `[data-wz-step="${step}"], #wizard-paso${step}, #paso${step}, #wizardPaso${step}, #wizard-step${step}, #step${step}`
    );
    if (!pane) { console.warn('gotoPaso: pane no encontrado', step); return; }

    // contenedor (tab-content) o documento
    const cont = pane.closest('.tab-content') || document;

    // ocultar todos y mostrar el requerido
    cont.querySelectorAll('.tab-pane').forEach(el => {
      el.classList.remove('active','show');
      el.setAttribute('aria-hidden','true');
    });
    pane.classList.add('active','show');
    pane.removeAttribute('aria-hidden');

    // marcar el tab nav si existe
    const tab = document.querySelector(`[data-bs-target="#${pane.id}"], a[href="#${pane.id}"]`);
    if (tab) {
      const nav = tab.closest('.nav') || document;
      nav.querySelectorAll('.nav-link.active').forEach(l=>l.classList.remove('active'));
      tab.classList.add('active');
    }
  };
})();

3) No tocar lo que ya está OK

Delegación para #wizard-finish y #wizard-confirm (sin clonado).

Rutas absolutas con helper api().

Paso 2 usa catálogo.

Paso 3: select “Asignado a” con placeholder “— Seleccioná —”.

4) (Info) El error “A listener indicated an asynchronous response…” es de una extensión del navegador; no es del código. Ignorable.
Verificación express (vos)

con el modal abierto:

({
  gotoPaso: typeof window.gotoPaso,                 // => "function"
  pane2: !!document.querySelector('[data-wz-step="2"],#wizard-paso2,#paso2,#wizardPaso2,#wizard-step2,#step2') // => true
})


probá la navegación manual:

window.gotoPaso(2);
document.querySelector('.tab-pane.active')?.id || 'sin active'  // debería ser el id del paso 2


seguí el flujo completo (2→3→Finalizar→4) y mirá Network: tiene que verse el POST de bulk-create y quedarse en Paso 4; al Confirmar, se cierra el modal (sin rebotar al 2).

si con eso aún no mueve, te paso un mini-parche para consola que fuerza el .active/.show del pane actual. Pero con el data-wz-step + gotoPaso que te dejé, debería quedar sólido.