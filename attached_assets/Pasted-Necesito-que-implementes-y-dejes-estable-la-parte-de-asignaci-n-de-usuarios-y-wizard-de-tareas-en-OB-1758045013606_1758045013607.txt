Necesito que implementes y dejes estable la parte de asignación de usuarios y wizard de tareas en OBYRA IA.
Te paso todo el detalle dividido en pasos, issues y criterios.

1️⃣ HOTFIX ya aplicado (no tocar)

El formulario de “Asignar Usuario” ahora funciona con submit tradicional.

No volver a poner preventDefault() hasta que el AJAX esté estable.

Si después querés implementar AJAX, hacerlo en una rama separada (feature/ajax-asignar-usuario).

2️⃣ Wizard de creación y asignación de tareas
➡️ Objetivo general

Armar un flujo donde, desde el detalle de una obra:

Selecciono etapas

Veo lista de tareas sugeridas para esas etapas y tildo las que quiero

Para cada tarea: completo inicio, fin, horas, unidad, responsable (miembro de la obra)

Confirmo y se crean todas juntas, asignando responsables en el mismo paso.

➡️ Issues / PRs que necesitamos
Issue #1 – Backend

Título: feat(api): Wizard – creación masiva de tareas
Descripción: Implementar endpoint para crear varias tareas y asignar responsables en un solo paso.

Endpoint: POST /obras/<obra_id>/wizard/tareas

Body esperado:

{
  "etapas": [
    {
      "etapa_id": 12,
      "tareas": [
        {
          "nombre": "Construcción de muros exteriores",
          "inicio": "2025-09-20",
          "fin": "2025-09-27",
          "horas_estimadas": 40,
          "unidad": "h",
          "responsable_id": 6
        }
      ]
    }
  ],
  "evitar_duplicados": true
}


Respuesta:

{
  "ok": true,
  "creadas": 7,
  "ya_existian": 2,
  "asignaciones_creadas": 5
}


Reglas:

Upsert por (etapa_id, nombre) si evitar_duplicados es true.

Validar que responsable_id sea miembro de la obra.

Insertar en tabla tarea_miembros si viene responsable_id.

Todo en transacción.

Issue #2 – Frontend/UI

Título: feat(ui): Wizard Etapa → Tareas → Datos → Confirmar
Descripción: Crear wizard en el detalle de obra con los siguientes pasos:

Paso 1: Seleccionar etapas (checkbox).

Paso 2: Tildar tareas sugeridas.

Paso 3: Completar tabla editable (inicio, fin, horas, unidad, responsable).

Paso 4: Resumen + Confirmar.

Validaciones:

Fechas obligatorias.

Horas > 0.

Responsable debe ser miembro.

Enviar todo el JSON al endpoint anterior.

Al confirmar:

Mostrar loading.

Si ok:true, cerrar wizard y refrescar tareas.

Permisos: visible solo para admin/pm.

Issue #3 – QA

Título: qa: Wizard de tareas y asignación
Descripción: Validar end-to-end el flujo.
Criterios de aceptación:

Se pueden crear tareas nuevas desde wizard.

Si ya existían, responde ya_existian sin duplicar.

Asigna responsables correctamente.

Todo queda visible en lista de tareas y en “Equipo Asignado”.

No rompe asignaciones manuales.

Respuesta JSON correcta y UI actualiza sin refresco forzado.

3️⃣ Ajustes en modelos y tablas (ya corregidos)

Tabla obra_miembros: columnas finales

id | obra_id | usuario_id | rol_en_obra | etapa_id | created_at


Modelo ObraMiembro debe usar usuario_id (no user_id).

Tabla tarea_miembros: tarea_id, usuario_id, cuota_objetivo con constraint único (tarea_id, usuario_id).

4️⃣ QA rápido (después de merge)

Asignar un usuario a la obra → debe aparecer en “Equipo Asignado”.

Abrir wizard:

Seleccionar una etapa.

Tildar 2 tareas.

Completar datos (fechas, horas, unidad, responsable).

Guardar.

Confirmar que:

Tareas aparecen con responsable.

En Network aparece POST /obras/<id>/wizard/tareas con el payload correcto.

Respuesta tiene ok:true.

5️⃣ Notas finales

Mantener logs claros en backend (logger.info).

En UI, mostrar errores legibles si falla validación o DB.

No mezclar código de wizard con otras features en el mismo PR.

💡 Resumen para título del PR final (una vez mergeado backend+UI):

feat(wizard): flujo Etapas → Tareas → Datos → Confirmar con asignación masiva